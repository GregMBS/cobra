<!DOCTYPE html>
<html>
<head>
<title>Administraci&oacute;n de las cuentas de los gestores</title>
<title>COBRA - Cambio de Status</title>
<link rel="stylesheet"
	href="https://code.jquery.com/ui/1.12.0/themes/redmond/jquery-ui.css"
	type="text/css" media="all" />
<script src="https://code.jquery.com/jquery-1.12.4.min.js"
	type="text/javascript"></script>
<script src="https://code.jquery.com/ui/1.12.0/jquery-ui.min.js"
	type="text/javascript"></script>
<style>
tr:hover {
	background-color: yellow;
}
</style>
</head>
<body>
	<button onclick="window.location = '/reports'">Regressar a la
		plantilla administrativa</button>
	<br>
	<table summary="Gestores" class="ui-widget">
		<thead class="ui-widget-header">
			<tr>
				<form action="/gestoradmin" method="get" name="migoorden">
					<th>Gestor</a></th>
					<th>Completo</a></th>
					<th>Contrase&ntilde;a</a></th>
					<th>Tipo</a></th>
			</tr>
		</thead>
		<tbody class="ui-widget-content">
			{% for r in result %}
			{% set rnd = random() %}
			<tr>

				<td><input form="gestorchange{{ rnd }}" type="text"
					name="iniciales" readonly="readonly" value="{{ r.iniciales }}" /></td>
				<td><input form="gestorchange{{ rnd }}" type="text"
					name="completo" value="{{ r.completo }}" /></td>
				<td><input form="gestorchange{{ rnd }}" type="password"
					name="passw" value="" /></td>
				<td><select form="gestorchange{{ rnd }}" name="tipo">
						<option value=""></option>
						{% set seltxt = '' %}
						{% for g in group %}
						{% if g == r.tipo %}
						{% set seltxt = 'selected' %}
						{% endif %}
						<option value="{{ g }}" style="font-size: 120%;" {{ seltxt }}>{{ g }}</option>
						{% endfor %}
				</select></td>
				<td><form name="gestorchange{{ rnd }}" method="get"
						action="/gestor/change" id="gestorchange{{ rnd }}">
						<input type="submit" name="go" value="GUARDAR">
					</form></td>
				<td><form name="gestordelete{{ rnd }}" method="get"
						action="/gestor/delete" id="gestordelete{{ rnd }}">
						<input type="hidden" name="iniciales" value="{{ r.iniciales }}" />
						<input type="submit" name="go" value="BORRAR">
					</form></td>
				</form>
			</tr>
			{% endfor %}
			<tr>
				<form name="gestoradd" method="get" action="/gestor/add"
					id="gestoradd">
					<td><input type="text" name="iniciales" value="" /></td>
					<td><input type="text" name="completo" value="" /></td>
					<td><input type="password" name="passw" value="" /></td>
					<td><select name="tipo">
							<option value=""></option>
							{% for g in group %}
							<option value="{{ g }}" style="font-size: 120%;">{{ g }}</option>
							{% endfor %}
					</select></td>
					<td><input type="submit" name="go" value="AGREGAR"></td>
				</form>
			</tr>
		</tbody>
	</table>
</body>
</html>
